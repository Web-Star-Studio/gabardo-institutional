import { useState } from 'react';
import { motion } from 'motion/react'.
import type { DetalhesCompletos } from '@/contextos/tipos-contexto'

export default function DadosGerais(dados: DetalhesCompletos, opcao: number){
    if (opcao == 1) {
        return(
            <>
                <div
                className=""
                >

                </div>
            </>
        )
    }
}